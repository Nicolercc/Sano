# Sano Accessibility Findings Log

This log uses the required evidence shape for every issue discovered during the scoped accessibility audit. Phase 2 remediation is recorded after the implementation commits and automated validation checks listed below.

| Component | User task | Problem | Who it affects | Standard | Severity | Decision | Evidence | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `SearchShell.tsx` | Understand the landing page and data source | Several small hero labels used low-opacity white text. Baseline contrast samples: `white/40` on `#1e2a38` was 3.60:1 and `white/45` was 4.17:1, both below 4.5:1 for normal text. | Low-vision users, users on bright or low-quality displays | WCAG 1.4.3 Contrast (Minimum) | High | Fixed: meaningful hero labels now use `text-white/72`; placeholder text remains intentionally excluded from persistent text checks. | Before: `screenshots/baseline/01-desktop-landing.png`, `screenshots/baseline/02-mobile-landing.png`. Code diff: `f9dd314`. | Automated: `npm run a11y:contrast` verifies `white/72` on `#1e2a38` at 8.22:1 and blocks reintroduction of risky white opacity text classes unless allowlisted. Manual desktop/mobile screenshot inspection still needed. |
| `SearchShell.tsx`, `RestaurantCard.tsx`, `RestaurantProfile.tsx`, `MethodologyPanel.tsx` | Scan secondary descriptions, helper text, labels, and metadata | Repeated `text-ink/45`, `text-ink/50`, and `text-ink/55` patterns fell below intended contrast on white/oat surfaces. Baseline samples: `ink/45` on oat was 2.75:1, `ink/50` on oat was 3.17:1, and `ink/55` on oat was 3.65:1. | Low-vision users, older users, users reading quickly under glare | WCAG 1.4.3 Contrast (Minimum) | High | Fixed: meaningful secondary/helper copy uses `text-ink/65` or stronger; decorative separators and placeholders are not treated as persistent meaningful text. | Before: `screenshots/baseline/03-search-filter-state.png`, `screenshots/baseline/04-search-results.png`, `screenshots/baseline/08-restaurant-profile.png`, `screenshots/baseline/11-methodology.png`. Code diff: `f9dd314`. | Automated: `npm run a11y:contrast` verifies `ink/65` on oat, white, and `--surface-2` at 5.01:1 or higher and blocks risky `text-ink/45`, `/50`, `/55`, and `/60` meaningful text classes. Manual journey screenshots still needed. |
| `FilterBar.tsx`, `SearchShell.tsx` | Use keyboard to search and refine filters | Text inputs and selects removed the default outline and mostly relied on border color changes. The landing search had a stronger ring, but filter controls needed equivalent visible keyboard focus. | Keyboard-only users, low-vision users, users with motor impairments | WCAG 2.4.7 Focus Visible | High | Fixed: filter input/select controls share `filterControlFocusClass` with border, outline, width, offset, and moss color focus-visible styling. | Before: `screenshots/baseline/10-keyboard-focus-landing-search.png`, `screenshots/baseline/03-search-filter-state.png`. Code diff: `33fece2`. | Automated: `npm run a11y:regression` verifies the shared focus helper includes outline tokens and is applied to all four filter input/select controls. Manual keyboard screenshot retest still needed. |
| `SearchShell.tsx` | Submit a landing search and continue with filters/results by keyboard | After hero search submission, focus remained in the hero flow instead of moving to the search/results context. The Phase 1 keyboard run recorded filter controls outside the viewport before the viewport caught up. | Keyboard-only users, low-vision users, users using screen magnification | WCAG 2.4.3 Focus Order; WCAG 2.4.7 Focus Visible; WCAG 2.4.11 Focus Not Obscured (Minimum) | High | Fixed: hero search submit scrolls to `#search` and moves focus to the search heading, which is programmatically focusable and has a visible focus style. | Before: `phase1-browser-audit.json`, `screenshots/phase1-keyboard/03-enter-submits-landing-search.png`. Code diff: `244d9ed`. | Automated: `npm run a11y:regression` verifies the focus target, `tabIndex={-1}`, focus call, and visible focus class. Manual keyboard-only retest from fresh `/` still needed. |
| `SearchShell.tsx` | Understand search progress, result counts, empty states, and failures without watching the screen | Search state changes were communicated visually through “Searching…”, result counts, empty-state copy, and error panels, but there was no deliberate live region announcing current search status after filters or hero search submission changed the result set. | Screen-reader users, low-vision users using assistive technology, users who need confirmation that a submitted search changed the page | WCAG 4.1.3 Status Messages; WCAG 1.3.1 Info and Relationships | High | Fixed: one concise `role="status"` region with `aria-live="polite"` and `aria-atomic="true"` announces loading, result counts, empty state, and error state. | Before: `screenshots/baseline/03-search-filter-state.png`, `screenshots/baseline/04-search-results.png`, `screenshots/baseline/05-empty-state.png`, `screenshots/baseline/07-error-state.png`. Code diff: `10248f3`. | Automated: `npm run a11y:regression` verifies exactly one `aria-live="polite"` and one `#search-status` region. Manual screen-reader announcement check still needed. |
| `SanoScorePanel.tsx` | Understand restaurant profile sections by structure | The score panel was a `<section>` without an accessible section heading. “Inspection reliability” visually acted as a heading but was rendered as paragraph text. | Screen-reader users, users navigating by headings/regions | WCAG 1.3.1 Info and Relationships; WCAG 2.4.6 Headings and Labels | Medium | Fixed: the panel is wired with `aria-labelledby` and “Inspection reliability” is rendered as an `h2`. | Before: `phase1-browser-audit.json`, `screenshots/baseline/08-restaurant-profile.png`. Code diff: `61a831a`. | Automated: `npm run lint` and `npm run build` passed after the semantic change. Manual semantic route audit still needed. |
| `app/restaurants/[id]/not-found.tsx` | Recover from an unavailable restaurant profile | The not-found boundary used a local `<nav>` without an accessible name, and its Methodology link lacked the explicit focus-visible style used in the primary app nav. | Screen-reader users, keyboard-only users | WCAG 1.3.1 Info and Relationships; WCAG 2.4.7 Focus Visible | Medium | Fixed: local nav now has `aria-label="Primary navigation"` and both recovery links use explicit focus-visible outlines aligned with the primary nav style. | Before: `phase1-browser-audit.json`. Code diff: `61a831a`. | Automated: `npm run check` and `npm run acceptance` passed with the missing-profile route. Manual keyboard-focus check for both recovery links still needed. |
| `SearchShell.tsx`, `TrustTimeline.tsx` | Read warning/status states in filters, empty state, and timeline | Amber status text and badges were under-contrast on light backgrounds. Baseline sample: `#d69d3f` on oat was 2.15:1 and on white was 2.40:1. | Low-vision users, color-sensitive users, users scanning status information | WCAG 1.4.3 Contrast (Minimum); WCAG 1.4.11 Non-text Contrast where badge boundaries convey state | Medium | Fixed: amber foreground text uses `amberText`; softer amber fill remains for backgrounds. Timeline repeat marker stroke uses the darker amber text token. | Before: `screenshots/baseline/03-search-filter-state.png`, `screenshots/baseline/05-empty-state.png`, `screenshots/baseline/09-inspection-timeline.png`. Code diff: `f9dd314`. | Automated: `npm run a11y:contrast` verifies `amberText` on oat at 6.38:1 and on white at 7.12:1 and blocks `text-amber` meaningful text. Manual visual inspection of amber states still needed. |
| `RestaurantCard.tsx`, `SanoScorePanel.tsx`, `TrustTimeline.tsx` | Interpret critical flags and reliability warnings | Coral foreground text could fall below 4.5:1 on light surfaces when used for normal-size text. Baseline sample: `#c8664c` on oat was 3.44:1 and on white was 3.85:1. | Low-vision users and users relying on warning/status text | WCAG 1.4.3 Contrast (Minimum) | Medium | Fixed: coral foreground text uses `coralText`; softer coral remains available for fills and accents. Timeline critical markers use the darker coral token. | Before: `screenshots/baseline/04-search-results.png`, `screenshots/baseline/08-restaurant-profile.png`, `screenshots/baseline/09-inspection-timeline.png`. Code diff: `f9dd314`. | Automated: `npm run a11y:contrast` verifies `coralText` on oat at 5.85:1 and on white at 6.53:1 and blocks `text-coral` meaningful text. Manual warning-state inspection still needed. |
| `TrustTimeline.tsx` | Understand inspection timeline patterns | The SVG timeline used color and small markers for critical flags, repeat patterns, and trends. Text badges below the chart helped, but the graphical relationship could still be hard to perceive at small sizes. | Low-vision users, color-blind users, screen magnifier users | WCAG 1.4.1 Use of Color; WCAG 1.4.11 Non-text Contrast | Medium | Improved: text badges remain, timeline critical and repeat markers now use darker audited status colors, and the SVG keeps its accessible label. Full perceptual review remains manual. | Before: `screenshots/baseline/09-inspection-timeline.png`. Code diff: `f9dd314`. | Automated: `npm run a11y:contrast` covers the status foreground tokens. Manual screen-reader, magnification, desktop/mobile visual inspection, and chart marker perception review still needed. |

## Finding Intake Template

Use this structure for any additional issues Claude or a reviewer discovers:

| Component | User task | Problem | Who it affects | Standard | Severity | Decision | Evidence | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `Component.tsx` | User goal | Specific accessibility problem | Affected users | WCAG criterion where applicable | High / Medium / Low | Chosen remediation | Before screenshot + code diff | Contrast calculation, automated result, and/or manual inspection |
